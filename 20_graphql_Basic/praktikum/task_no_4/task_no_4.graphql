mutation InputAnggota {
  insert_kampus_merdeka_anggota_one(object: { nama: "Budi" }) {
    id
    nama
  }
}

mutation InputAnggota {
  insert_kampus_merdeka_anggota_one(object: { nama: "Sita" }) {
    id
    nama
  }
}

mutation InputAnggota {
  insert_kampus_merdeka_anggota_one(object: { nama: "Angga" }) {
    id
    nama
  }
}
